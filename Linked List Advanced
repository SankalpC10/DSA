1.Reverse a Linked List in groups
  Link:https://practice.geeksforgeeks.org/problems/reverse-a-linked-list-in-groups-of-given-size/1
  Explanation:
    We use revrse link list property to reverse List in size of k.
  Code:
    struct node *reverse (struct node *head, int k)
    { 
        node *curr=head;
        node *prevFirst=NULL;
        bool isFirstPass=true;
        while(curr!=NULL)
        {
            node *first =curr, *prev=NULL;
            int count=0;
            while(curr!=NULL && count<k)
            {
                node *next=curr->next;
                curr->next=prev;
                prev=curr;
                curr=next;
                count++;
            }
            if(isFirstPass)
            {
                head=prev;
                isFirstPass=false;
            }
            else{
                prevFirst->next=prev;
            }
            prevFirst=first;
        }
        return head;
    }
